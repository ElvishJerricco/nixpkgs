diff --git a/src/bootctl/bootctl-install.c b/src/bootctl/bootctl-install.c
index 16ae5de99f..1aa637622d 100644
--- a/src/bootctl/bootctl-install.c
+++ b/src/bootctl/bootctl-install.c
@@ -376,7 +376,7 @@
         if (s)
                 *s = 0;
 
-        p = path_join(BOOTLIBDIR, name);
+        p = path_join(arg_boot_lib_dir ?: BOOTLIBDIR, name);
         if (!p)
                 return log_oom();
 
@@ -433,16 +433,16 @@
         _cleanup_free_ char *path = NULL;
         int r;
 
-        r = chase_and_opendir(BOOTLIBDIR, root, CHASE_PREFIX_ROOT|CHASE_PROHIBIT_SYMLINKS, &path, &d);
+        r = chase_and_opendir(arg_boot_lib_dir ?: BOOTLIBDIR, root, CHASE_PREFIX_ROOT|CHASE_PROHIBIT_SYMLINKS, &path, &d);
         /* If we had a root directory to try, we didn't find it and we are in auto mode, retry on the host */
         if (r == -ENOENT && root && arg_install_source == ARG_INSTALL_SOURCE_AUTO)
-                r = chase_and_opendir(BOOTLIBDIR, NULL, CHASE_PREFIX_ROOT|CHASE_PROHIBIT_SYMLINKS, &path, &d);
+                r = chase_and_opendir(arg_boot_lib_dir ?: BOOTLIBDIR, NULL, CHASE_PREFIX_ROOT|CHASE_PROHIBIT_SYMLINKS, &path, &d);
         if (r == -ENOENT && arg_graceful) {
                 log_debug("Source directory does not exist, ignoring.");
                 return 0;
         }
         if (r < 0)
-                return log_error_errno(r, "Failed to open boot loader directory %s%s: %m", strempty(root), BOOTLIBDIR);
+                return log_error_errno(r, "Failed to open boot loader directory %s%s: %m", strempty(root), arg_boot_lib_dir ?: BOOTLIBDIR);
 
         const char *suffix = strjoina(arch, ".efi");
         const char *suffix_signed = strjoina(arch, ".efi.signed");
diff --git a/src/bootctl/bootctl.c b/src/bootctl/bootctl.c
index 0c1ee867d7..7233fc9489 100644
--- a/src/bootctl/bootctl.c
+++ b/src/bootctl/bootctl.c
@@ -69,6 +69,7 @@
 char *arg_private_key = NULL;
 KeySourceType arg_private_key_source_type = OPENSSL_KEY_SOURCE_FILE;
 char *arg_private_key_source = NULL;
+char *arg_boot_lib_dir = NULL;
 
 STATIC_DESTRUCTOR_REGISTER(arg_esp_path, freep);
 STATIC_DESTRUCTOR_REGISTER(arg_xbootldr_path, freep);
@@ -82,6 +83,7 @@
 STATIC_DESTRUCTOR_REGISTER(arg_certificate_source, freep);
 STATIC_DESTRUCTOR_REGISTER(arg_private_key, freep);
 STATIC_DESTRUCTOR_REGISTER(arg_private_key_source, freep);
+STATIC_DESTRUCTOR_REGISTER(arg_boot_lib_dir, freep);
 
 int acquire_esp(
                 int unprivileged_mode,
@@ -369,6 +371,7 @@
                 ARG_CERTIFICATE_SOURCE,
                 ARG_PRIVATE_KEY,
                 ARG_PRIVATE_KEY_SOURCE,
+                ARG_BOOTLIBDIR,
         };
 
         static const struct option options[] = {
@@ -405,6 +408,7 @@
                 { "certificate-source",          required_argument, NULL, ARG_CERTIFICATE_SOURCE          },
                 { "private-key",                 required_argument, NULL, ARG_PRIVATE_KEY                 },
                 { "private-key-source",          required_argument, NULL, ARG_PRIVATE_KEY_SOURCE          },
+                { "boot-lib-dir",                required_argument, NULL, ARG_BOOTLIBDIR                  },
                 {}
         };
 
@@ -600,6 +604,11 @@
                                 return r;
                         break;
 
+                case ARG_BOOTLIBDIR:
+                        r = free_and_strdup_warn(&arg_boot_lib_dir, optarg);
+                        if (r < 0)
+                                return r;
+                        break;
                 case '?':
                         return -EINVAL;
 
diff --git a/src/bootctl/bootctl.h b/src/bootctl/bootctl.h
index 191a970cd7..f1ad4e5b0e 100644
--- a/src/bootctl/bootctl.h
+++ b/src/bootctl/bootctl.h
@@ -46,6 +46,7 @@
 extern char *arg_private_key;
 extern KeySourceType arg_private_key_source_type;
 extern char *arg_private_key_source;
+extern char *arg_boot_lib_dir;
 
 static inline const char* arg_dollar_boot_path(void) {
         /* $BOOT shall be the XBOOTLDR partition if it exists, and otherwise the ESP */
